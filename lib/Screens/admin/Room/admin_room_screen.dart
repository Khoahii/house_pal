import 'package:flutter/material.dart';

class AdminRoomScreen extends StatelessWidget {
  const AdminRoomScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Admin Room')));
  }
}
